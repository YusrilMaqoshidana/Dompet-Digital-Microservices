# sharding.yaml
mode:
  type: Standalone
  repository:
    type: JDBC

dataSources:
  ds0:
    dataSourceClassName: com.zaxxer.hikari.HikariDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    jdbcUrl: jdbc:mysql://ds0:3306/db_1?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: secret
  ds1:
    dataSourceClassName: com.zaxxer.hikari.HikariDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    jdbcUrl: jdbc:mysql://ds1:3306/db_2?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: secret
#  ds2:
#    dataSourceClassName: com.zaxxer.hikari.HikariDataSource
#    driverClassName: org.postgresql.Driver
#    jdbcUrl: jdbc:postgresql://ds2:5432/db_3
#    username: postgres
#    password: secret

rules:
  - !SHARDING
    tables:
      t_user:
        actualDataNodes: ds${0..1}.t_user
    defaultDatabaseStrategy:
      standard:
        shardingColumn: user_id
        shardingAlgorithmName: database_inline
    defaultTableStrategy:
      none:
    shardingAlgorithms:
      database_inline:
        type: INLINE
        props:
          algorithm-expression: ds${user_id.hashCode().abs() % 2}

props:
  sql-show: false